# Instrucciones para el agente

- Antes de finalizar una tarea, verifica que la página compile y se vea correctamente ejecutando `npm run dev` y revisando el resultado en el navegador.
- No des por sentado que un cambio es correcto solo porque el código se ve bien. Comprueba visualmente el funcionamiento.
